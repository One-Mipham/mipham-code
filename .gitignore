node_modules/
dist/
.next/
out/
*.log
.env
.env.local
.mipham/
.claude/worktrees/
.claude/plans/
.playwright-mcp/
*.tgz
*.vsix
.vercel
.worktrees/
.cdp-token

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
# graft writes un-anchored `graft/`, which also matches the skill dir
# `.claude/skills/graft/` — re-include it so the skill stays committable.
!/.claude/skills/graft/

# Claude Code local settings (may contain secrets)
.claude/settings.local.json
